#pragma once

enum class direction {
	NORTH,
	EAST,
	SOUTH,
	WEST,
	STAY
};

enum class font {
	TITLE,
	SUBTITLE,
	TEXT,
	OPTION
};

enum class shape {
	TEXT,
	CURSOR,
	RECT,
	CIRCLE,
	SQUARE,
	ELLIPSE
};